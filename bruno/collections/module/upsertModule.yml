info:
  name: upsertModule
  type: http
  seq: 2

http:
  method: PUT
  url: "{{url}}/api/v1/agents/{{id_agent}}/modules/{{module_key}}"
  body:
    type: json
    data: |-
      {
        "enabled": true,
        "priority": 2,
        "config": {
          "systemPrompt": "Eres un agente de la direccion de carrera de ingenieria informatica y sistemas, responde de manera formal, inventate horarios si alguien te pregunta",
          
          "model":"openrouter/owl-alpha"
        }
      }
  auth: inherit

runtime:
  variables:
    - name: id_agent
      value: 234af589-3b13-42fc-a8b9-49c33ba756a4
    - name: module_key
      value: developer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
