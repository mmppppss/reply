info:
  name: listLogs
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/v1/developer/logs?idAgent={{id_agent}}"
  auth:
    type: bearer
    token: "{{token_jwt}}"

runtime:
  variables:
    - name: id_agent
      value: 234af589-3b13-42fc-a8b9-49c33ba756a4
    - name: jwt_token
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
