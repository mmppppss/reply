info:
  name: GetSessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/users/{{id_user}}/whatsapp/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
