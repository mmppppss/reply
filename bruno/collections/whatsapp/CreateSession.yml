info:
  name: CreateSession
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/users/{{id_user}}/whatsapp/connect"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
