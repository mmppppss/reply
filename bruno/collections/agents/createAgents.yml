info:
  name: createAgents
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/user/{{id_user}}/agents/"
  body:
    type: json
    data: |-
      {
        "name":"Prueba",
        "description":"Tu tienda de plantas de confianza"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
