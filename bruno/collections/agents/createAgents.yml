info:
  name: createAgents
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/user/{{id_user}}/agents/"
  body:
    type: json
    data: |-
      {
        "name":"Informatica",
        "description":"Direccion de informatica"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
