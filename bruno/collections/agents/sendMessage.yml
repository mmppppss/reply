info:
  name: sendMessage
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/v1/user/{{id_user}}/agents/{{id_agent}}/send"
  body:
    type: json
    data: |-
      {
        "provider": "whatsapp",
        "to":"59171688979@s.whatsapp.net",
        "text":"hola que tal"
      }
  auth: inherit

runtime:
  variables:
    - name: id_agent
      value: 91f76f63-5bde-4aa9-837a-f6bccab6d624

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
