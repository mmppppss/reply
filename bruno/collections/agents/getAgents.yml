info:
  name: getAgents
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/user/{{id_user}}/agents/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
