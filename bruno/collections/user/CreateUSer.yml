info:
  name: CreateUSer
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/user/create"
  body:
    type: json
    data: |-
      {
        "username": "pedrotest",
        "password": "pedro123",
        "email": "p.pozosoliz@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
